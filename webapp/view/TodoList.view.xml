<mvc:View
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
   <List
      headerText="ToDos"
      class="sapUiResponsiveMargin"
      width="auto"
      items="{/todos}" >
      <items>
         <ObjectListItem
            title="{title}"/>
      </items>
   </List>
</mvc:View>